addSubjectImage <- function(
  imageDataList, 
  id, 
  filename, 
  directory, 
  image.type, 
  registered = TRUE, 
  template = NULL, 
  type = "nifti", 
  verbose = TRUE
){
  if (type != "nifti") stop("Only nifti supported at this time")
}

